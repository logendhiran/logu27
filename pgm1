    #include<iostream.h>
    #include<conio.h>
    void main()
    {
    	int n;
    	cout<<"enter the number:");
    	cin>>n;
    	if(n>0)
    	cout<<"the number is positive";
    	else if(n<0)
    	cout<<"the number is negative";
    	else
    	cout<<"the number is zero";
    	getch();
    }
